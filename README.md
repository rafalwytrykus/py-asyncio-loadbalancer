# Python asyncio load balancer

### Run instructions
The test suite can be run in Docker using the provided Dockerfile:

1. Build the image
```
docker build . -t py-async-loadbalancer
```

2. Run container
```
docker run py-async-loadbalancer
```
